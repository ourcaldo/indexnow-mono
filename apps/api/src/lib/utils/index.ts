// Utilities & Helper Functions
export { 
  cn, 
  formatCurrency, 
  formatDate, 
  formatRelativeTime, 
  formatNumber,
  truncateString,
  capitalizeFirstLetter,
  countries, 
  findCountryByCode,
  getPopularCountries,
  removeUrlParameters,
  extractDomain,
  normalizeUrl,
  isValidUrl,
  ensureProtocol,
  getClientIP,
  parseUserAgent,
  getRequestInfo,
  formatDeviceInfo,
  formatLocationData,
  getSecurityRiskLevel
} from '@indexnow/shared'

export type { Country, DeviceInfo, LocationData } from '@indexnow/shared'