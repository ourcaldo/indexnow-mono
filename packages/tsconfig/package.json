{
  "name": "@indexnow/tsconfig",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript configs. (#V7 L-40) Only ships nextjs.json; library packages extend ../../tsconfig.base.json directly instead of a separate preset.",
  "files": [
    "nextjs.json"
  ]
}
